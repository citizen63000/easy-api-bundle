{{ route_name_prefix }}_{{ entity_route_name }}_get:
    path: /{{ entity_url_name }}/{id}
    defaults: { _controller: {{ routingControllerPath }}:get }
    methods: GET
    requirements:
        id: '\d+'

{{ route_name_prefix }}_{{ entity_route_name }}_list:
    path: /{{ entity_url_name }}
    defaults: { _controller: {{ routingControllerPath }}:list }
    methods: GET

{{ route_name_prefix }}_{{ entity_route_name }}_create:
    path: /{{ entity_url_name }}
    defaults: { _controller: {{ routingControllerPath }}:create }
    methods: POST

{{ route_name_prefix }}_{{ entity_route_name }}_update:
    path: /{{ entity_url_name }}/{id}
    defaults: { _controller: {{ routingControllerPath }}:update }
    methods: PUT
    requirements:
        id: '\d+'

{{ route_name_prefix }}_{{ entity_route_name }}_delete:
    path: /{{ entity_url_name }}/{id}
    defaults: { _controller: {{ routingControllerPath }}:delete }
    methods: DELETE
    requirements:
        id: '\d+'

{{ route_name_prefix }}_{{ entity_route_name }}_describe_form:
    path: /{{ entity_url_name }}/describeForm
    defaults: { _controller: {{ routingControllerPath }}:describeForm }
    methods: GET
