<?php

namespace {{ namespace }};

use Swagger\Annotations as SWG;
{% for use in uses %}
use {{ use }};
{% endfor %}

/**
 * @SWG\Tag(name="{{ entity_name }}")
 */
class {{ entity_name }}Controller extends {{ parent }}
{
    public const entityClass = {{ entity_name }}::class;
    public const serializationGroups = [{{ serialization_groups|raw }}];
    public const entityCreateTypeClass = {{ entity_name }}Type::class;
    public const entityUpdateTypeClass = {{ entity_name }}Type::class;
    public const listSerializationGroups = [{{ serialization_groups|raw }}];
    public const filterFields = [{{ nativeFieldsNames|raw }}];
    public const filterSortFields = [{{ nativeFieldsNames|raw }}];

    use CrudControllerTrait;
}